pip install torch --index-url https://download.pytorch.org/whl/cu126
pip install -r requirements.txt
pip install -e .

# Depuis le dossier sportinsight_dense_anchor/

  # Une seule expérience (test rapide)
  python -m sportinsight.train --config configs/default.yaml

  # Toutes les 20 expériences en séquence
  python scripts/run_experiments.py --all

  # Reprendre après interruption
  python scripts/run_experiments.py --all --skip-existing

  # Graphes de comparaison (après au moins 2 runs terminés)
  python scripts/compare_experiments.py --all --output plots/comparison



LANCER BACKEND
uvicorn apps.api.main:app --reload --host 127.0.0.1 --port 8000

LANCER FRONTEND
cd sportinsight_dense_anchor\apps\web
npm install
npm run dev

TELECHARGER VIDEOS (100+ GO)
python scripts/download_soccernet_videos.py `
  --root data/SoccerNet `
  --password "s0cc3rn3t" `
  --resolution 224p `
  --split train valid test `
  --include-features `
  --include-labels
  
CREER SPLITS
python scripts/create_splits.py `
--root data/SoccerNet `
--output-dir splits `
--train-ratio 0.70 `
--valid-ratio 0.15 `
--test-ratio 0.15 `
--seed 42 `
--complete-halves